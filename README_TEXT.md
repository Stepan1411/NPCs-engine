# NPC Text System - Плавная печать текста

## 🎯 Использование

### Простой вызов
Вызовите функцию с вашим текстом:
```mcfunction
function npcs:text/show {text:"Ваш текст здесь"}
```

### Примеры
```mcfunction
# Простое приветствие
function npcs:text/show {text:"Привет!"}

# Длинный текст
function npcs:text/show {text:"Добро пожаловать в наш мир!"}

# С эмодзи
function npcs:text/show {text:"Привет, путник! 👋"}
```

## ✨ Особенности

- **Плавная печать** - текст появляется посимвольно с курсором █
- **Звуковые эффекты** - звук печати каждые 2 тика
- **Следует за игроком** - text_display остаётся над головой
- **Автоудаление** - текст исчезает через 5 секунд после завершения
- **Работает через макросы** - любой текст без предварительной настройки

## 🎮 Тестирование

Запустите тестовую функцию:
```mcfunction
/function npcs:text/test
```

## 🔧 Технические детали

- Использует `text_display` entity
- Поддерживает до 20 символов (легко расширяется)
- Скорость: 1 символ каждые 2 тика
- Позиция: 2 блока над игроком
- Billboard: center (всегда повёрнут к камере)

## 📝 Интеграция

Вы можете вызвать эту функцию из любого места:
```mcfunction
# В диалоге NPC
execute as @a[distance=..5] run function npcs:text/show {text:"Здравствуй!"}

# При событии
execute if score trigger value matches 1 run function npcs:text/show {text:"Событие активировано!"}
```
