# NPC Engine

**Система интерактивных NPC с диалогами для Minecraft 1.21.9+**

NPC Engine — датапак для создания NPC с системой диалогов, выбором ответов и кинематографической камерой.

---

## 🎯 Возможности

### NPC
- **Манекены** — NPC на основе `minecraft:mannequin` с кастомными скинами
- **Другие сущности** — жители, зомби, скелеты и др.
- **Интерактивность** — клик по NPC запускает диалог
- **Визуальный редактор** — управление через actionbar

### Диалоги
- **Эффект печатной машинки** — плавный посимвольный набор текста
- **Система выбора** — ветвление диалогов с несколькими вариантами ответа
- **Навигация** — Shift для перехода к следующей реплике
- **Text Display** — текст отображается над NPC

### Камера
- **Кинематографический режим** — автоматическое приближение к NPC
- **Позиционирование** — камера располагается сбоку от NPC
- **Плавные переходы** — эффект слепоты при начале/конце диалога
- **Восстановление** — автоматический возврат игрока на исходную позицию

---

## 🚀 Установка

1. Поместите папку `npcs-engine` в `datapacks` вашего мира
2. Выполните `/reload`

---

## 📋 Использование

### Открыть редактор
```mcfunction
/function npcs:editor/open
```

### Управление редактором
- **Колесико мыши** — навигация по меню
- **ПКМ (морковка на удочке)** — выбор пункта

### Меню редактора
| Пункт | Описание |
|-------|----------|
| 📝 Создать NPC | Создание нового NPC |
| 📋 Список | Просмотр всех NPC |
| 💬 Диалоги | Редактор диалогов |
| 🎬 Тест диалога | Запуск тестового диалога |
| ❌ Выход | Закрыть редактор |

### Взаимодействие с NPC
- Кликните по NPC для начала диалога
- **Shift** — следующая реплика
- Диалог завершается автоматически после последней строки

---

## 🔧 API для разработчиков

### Создание NPC
```mcfunction
# Создать NPC-манекена
function npcs:npc/create_player

# Создать NPC-сущность
function npcs:npc/create_entity
```

### Управление диалогами
```mcfunction
# Добавить строку диалога
function npcs:dialog/editor/add_line {npc_id:1,text:"Привет, путник!"}

# Добавить выбор
function npcs:dialog/editor/add_choice {npc_id:1,text:"Куда пойдём?",choices:["На север","На юг"]}

# Запустить диалог программно
function npcs:dialog/start_play
```

### Хранение данных
- **Storage** `npcs:data` — данные NPC и диалогов
- **Storage** `npcs:current_dialog` — текущий активный диалог

---

## 📊 Scoreboards

| Scoreboard | Назначение |
|------------|------------|
| `npc.id` | ID сущности NPC |
| `npc.dialog_state` | Состояние диалога (0 = неактивен) |
| `npc.dialog_line` | Текущая строка диалога |
| `npc.choice_state` | Состояние выбора |
| `npc.choice_selected` | Выбранный вариант |
| `npc.camera_state` | Состояние камеры |

---

## 📁 Структура

```
npcs-engine/
├── data/npcs/function/
│   ├── core/          # Загрузка и tick
│   ├── editor/        # Визуальный редактор
│   ├── npc/           # Создание и управление NPC
│   ├── dialog/        # Система диалогов
│   │   ├── choice/    # Система выбора
│   │   └── editor/    # Редактор диалогов
│   ├── camera/        # Кинематографическая камера
│   └── text/          # Text Display и набор текста
└── pack.mcmeta
```

---

## 📋 Требования

- **Minecraft**: 1.21.9+ (pack_format 88)
- **Права**: Оператор для команд редактора

---

## 🎮 Пример: Квестовый NPC

```mcfunction
# 1. Создать NPC
function npcs:npc/create_player

# 2. Добавить диалоги (npc_id = 1)
function npcs:dialog/editor/add_line {npc_id:1,text:"Приветствую, герой!"}
function npcs:dialog/editor/add_line {npc_id:1,text:"У меня есть для тебя задание."}
function npcs:dialog/editor/add_choice {npc_id:1,text:"Примешь ли ты его?",choices:["Конечно!","Не сейчас"]}
```

---

**Создавайте живые миры с NPC Engine!** 💬✨
